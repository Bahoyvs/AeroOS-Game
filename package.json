{
  "name": "aeroos-game",
  "version": "0.1.0",
  "description": "AeroOS - The Messenger Era. A Frutiger Aero idle/simulation game for the browser.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "7.css": "^0.16.0"
  },
  "devDependencies": {
    "vite": "^8.0.0",
    "vitest": "^3.0.0"
  }
}
