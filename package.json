{
  "name": "aeroos-game",
  "version": "0.2.0",
  "description": "AeroOS - The Messenger Era. A Frutiger Aero idle/simulation game for the browser.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "thumbnail": "node art/render-thumbnail.mjs",
    "wallpapers": "node art/optimize-wallpapers.mjs",
    "icons": "node art/optimize-icons.mjs",
    "check": "npm run test && npm run build",
    "package": "npm run check && powershell -ExecutionPolicy Bypass -File art/package-build.ps1"
  },
  "dependencies": {
    "7.css": "^0.16.0"
  },
  "devDependencies": {
    "vite": "^8.0.0",
    "vitest": "^3.0.0"
  }
}
